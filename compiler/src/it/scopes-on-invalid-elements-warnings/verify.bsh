import dagger.testing.it.BuildLogValidator;
import java.io.File;

File buildLog = new File(basedir, "build.log");
new BuildLogValidator().assertHasText(buildLog, new String[]{
    "Guice will ignore scoping annotations on methods that are not @Provides methods: test.TestApp.TestClass1.nonProvidesMethod()"});
new BuildLogValidator().assertHasText(buildLog, new String[]{
    "Guice will ignore scoping annotations on methods that are not @Provides methods: test.TestApp.TestClass2.nonProvidesMethod()"});
